/*--------------------- components alert ------------------------------------*/
var alert_del = document.querySelectorAll(".alert-success");
alert_del.forEach((x) =>
  x.addEventListener("click", function () {
    x.parentElement.classList.add("hidden");
  })
);

var alert_del = document.querySelectorAll(".alert-info");
alert_del.forEach((x) =>
  x.addEventListener("click", function () {
    x.parentElement.classList.add("hidden");
  })
);
var alert_del = document.querySelectorAll(".alert-primary");
alert_del.forEach((x) =>
  x.addEventListener("click", function () {
    x.parentElement.classList.add("hidden");
  })
);
var alert_del = document.querySelectorAll(".alert-danger");
alert_del.forEach((x) =>
  x.addEventListener("click", function () {
    x.parentElement.classList.add("hidden");
  })
);
var alert_del = document.querySelectorAll(".alert-warning");
alert_del.forEach((x) =>
  x.addEventListener("click", function () {
    x.parentElement.classList.add("hidden");
  })
);
var alert_del = document.querySelectorAll(".alert-dark");
alert_del.forEach((x) =>
  x.addEventListener("click", function () {
    x.parentElement.classList.add("hidden");
  })
);
